name: Debugging

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        uses: author/action-publish@master
        with:
          REGISTRY_TOKEN: "test"
          ACTIONS_STEP_DEBUG: true