name: Debugging

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        uses: author/action-publish@master
        with:
          scan: "../../, ../../, ../blahh"
          force: true
          ACTIONS_STEP_DEBUG: true
        env:
          REGISTRY_TOKEN: test